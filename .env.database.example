# A managed PostgreSQL connection string (Neon, Supabase, Vercel Postgres, etc.).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# ducnb17@gmail.com is the secure default; set explicitly in every deployment.
ADMIN_EMAIL="ducnb17@gmail.com"
